{
  "name": "my-movies-collection",
  "scripts": {
    "dev": "nodemon server.js",
    "test": "NODE_ENV=test jest --verbose --colors 2>&1",
    "migrate:up": "knex --cwd server migrate:up",
    "migrate:down": "knex --cwd server migrate:down"
  },
  "nodemonConfig": {
    "ignore": [
      "public/**",
      "charts/**"
    ]
  },
  "dependencies": {
    "axios": "^1.3.3",
    "better-sqlite3": "^8.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "knex": "^2.4.2"
  },
  "devDependencies": {
    "axios-mock-adapter": "^1.21.2",
    "connect-livereload": "^0.6.1",
    "livereload": "^0.9.3",
    "jest": "^29.4.3",
    "nodemon": "^2.0.20"
  }
}
